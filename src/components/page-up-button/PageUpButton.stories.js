import React from 'react';
import PageUpButton from './PageUpButton';

export default {
  title: 'Buttons/Page Up Button',
  component: PageUpButton,
};
const Template = (args) => <PageUpButton {...args} />;

export const Default = Template.bind({});
Default.args = {
};
